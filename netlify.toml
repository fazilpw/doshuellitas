# netlify.toml - CONFIGURACIÓN CORREGIDA PARA CLUB CANINO

[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# ============================================
# 🚀 REDIRECCIONES PARA SPA/ASTRO
# ============================================

# Redirección principal para SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  conditions = {Role = ["*"]}

# Redirecciones específicas para rutas protegidas
[[redirects]]
  from = "/app/*"
  to = "/app/index.html"
  status = 200

[[redirects]]
  from = "/dashboard/*"
  to = "/dashboard/index.html"
  status = 200

# ============================================
# 🛡️ HEADERS DE SEGURIDAD
# ============================================

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    # CSP más permisivo para desarrollo
    Content-Security-Policy = "default-src 'self' 'unsafe-inline' 'unsafe-eval' data: blob:; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdnjs.cloudflare.com https://*.supabase.co; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https://* blob:; connect-src 'self' https://* wss://*;"

# Headers específicos para archivos estáticos
[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000"

[[headers]]
  for = "/_astro/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000"

# ============================================
# 🔧 CONFIGURACIÓN DE FUNCIONES
# ============================================

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# ============================================
# 🌍 CONFIGURACIÓN DE ENTORNO
# ============================================

[context.production.environment]
  NODE_VERSION = "18"

[context.deploy-preview.environment]
  NODE_VERSION = "18"

[context.branch-deploy.environment]
  NODE_VERSION = "18"

# ============================================
# 🚀 CONFIGURACIÓN DEL ENTORNO DE DESARROLLO
# ============================================

[dev]
  command = "npm run dev"
  port = 4321
  targetPort = 4321
  publish = "dist"
  framework = "astro"