---
// src/pages/app.astro - CLIENT-ONLY FIX
import Layout from '../layouts/Layout.astro';
import { AuthProvider } from '../components/auth/AuthProvider.jsx';
import AppEntry from '../components/AppEntry.jsx';
---

<Layout title="Club Canino Dos Huellitas - App">
  <!-- USAR client:only="react" para evitar SSR por completo -->
  <AuthProvider client:only="react">
    <AppEntry client:only="react" />
  </AuthProvider>
</Layout>