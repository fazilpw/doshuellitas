---
// src/pages/login.astro
// 🔐 PÁGINA DE LOGIN CON DIRECTIVA CLIENT-ONLY
// Esta página garantiza que React solo se ejecute del lado del cliente

import Layout from '../layouts/Layout.astro';
import { ClientOnlyLoginPage } from '../components/auth/ClientOnlyAuthGuard.jsx';
---

<Layout title="Login - Club Canino Dos Huellitas">
  <!-- 
    🚀 CRÍTICO: client:only="react" garantiza que este componente
    NUNCA se ejecute durante SSR, solo del lado del cliente
  -->
  <ClientOnlyLoginPage client:only="react" />
</Layout>